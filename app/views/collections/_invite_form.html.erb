<%= form_for @invite , :url => invites_path do |f| %>
    <%= f.hidden_field :collection_id, :value => @invite.collection_id %>
    <%= f.label :email %>
    <%= f.email_field :email %>
    <%= f.submit 'Send Invite' %>
<% end %>
