


<nav class="navbar" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <%= link_to "Mement'ohs", root_path %>
  </div>
  <a role="button" class="navbar-burger" data-target="navMenu" aria-label="menu" aria-expanded="false">
    <span aria-hidden="true"></span>
    <span aria-hidden="true"></span>
    <span aria-hidden="true"></span>
  </a>
  <div class="navbar-menu" id="navMenu">
      <span aria-hidden="true">
        <%= link_to "Create", new_collection_path, class: "navbar-item" %>
      </span>
      <span aria-hidden="true">
        <% if current_user %>
        <div class="navbar-item has-dropdown is-hoverable">
          <%= link_to "View Collection", collections_path, class: "navbar-link" %>
          <% if current_collections.any? %>
          <div class="navbar-dropdown">
            <% current_collections.each do |collection| %>
            <%= link_to "#{collection.name}", collection_path(collection), class: "navbar-item" %>
            <% end %>
          </div>
          <% end %>
        </div>
        <% end %>
      </span>
    <% if current_user %>
      <span aria-hidden="true">
        <%= link_to "Sign out", '/users/sign_out', method: 'delete', class: "navbar-item" %>
      </span>
      <span aria-hidden="true">
        <%=link_to "Hi, #{current_user.username}", user_path(current_user), class: "navbar-item" %>
      </span>
    <% else %>
      <span aria-hidden="true">
        <%= link_to "Sign up", '/users/sign_up', class: "navbar-item"  %>
      </span>
      <span aria-hidden="true">
        <%= link_to "Sign in", '/users/sign_in', class: "navbar-item" %>
      </span>
    <% end %>
</nav>
